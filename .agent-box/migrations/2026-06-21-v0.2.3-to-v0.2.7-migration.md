# Brownfield-Migration: box-python v0.2.3 → v0.2.7

Status: abgeschlossen
Brownfield-Fall: B
Ausgangsversion: v0.2.3
Zielversion: v0.2.7
Datum: 2026-06-21
Freigabe: Dieter Haag (Plan docs/plans/2026-06-21-brownfield-migration-v0.2.3-to-v0.2.7.md,
          volle Schreiberlaubnis für alle Governance-Dateien)

## Baseline

Erhoben auf Branch `brownfield/v0.2.3-to-v0.2.7` VOR jeder Änderung.
Projekt-Baseline vollständig grün (tmp/brownfield/ aus Scope ausgeschlossen):

```text
python tools/check_agent_docs_consistency.py --instantiated   → OK (instantiated)
python tools/check_import_layers.py --preflight src tests tools → OK (38 Dateien)
python tools/resolve_test_obligations.py --selfcheck --instantiated → OK
python -m ruff check . --exclude tmp                          → All checks passed
python -m ruff format --check . --exclude tmp                 → 38 files already formatted
python -m mypy src                                            → Success, 22 source files
python -m pytest                                              → 64 passed
```

Anmerkung: `python -m ruff check .` / `ruff format --check .` ohne `--exclude tmp`
melden Fehler ausschließlich in `tmp/brownfield/agent-templates-main/` (entpacktes
Template, Scratch-Material, nicht Teil des Projekts). `tmp/brownfield/` bleibt
untracked und wird nach Abschluss entfernt. Es gibt keine roten Checks im Projektcode.

## Observed State

- Box am 2026-06-20 mit box-python v0.2.3 instanziiert (.agent-box/instantiation.md).
- Herkunftsmarker: genau einer (.agent-box/instantiation.md), kein adoption.md.
- glossar-domain.md / glossar-system.md lokal mit echten Wetter-Fachbegriffen gefüllt
  (310 / 223 Zeilen vs. 147 Template). Struktur template-kompatibel (## 3. Begriffe).
- learning-matrix.md lokal mit echten Mustern gefüllt (346 vs. 151 Zeilen).
- glossar-meta.md lokal handgemacht (commit c307ebf), abweichende Struktur
  (## 2. Begriffe), ohne Brownfield-Metabegriffe.
- Tools lokal nach Instanziierung angepasst (commits 495dd0b, b2a2b5b, 7e3e96f).
- Fehlend ggü. v0.2.7: BROWNFIELD-MIGRATION.md, .agent-box/migrations/ (jetzt angelegt).

## Datei-Aktionsmatrix

Siehe Plan docs/plans/2026-06-21-brownfield-migration-v0.2.3-to-v0.2.7.md (Aktionsmatrix).
Zusammenfassung: add (BROWNFIELD-MIGRATION.md) · merge (Regeldokumente, Glossare
preserve-dominant, Tools) · preserve (README, MODELL-README, agent-box-instantiation)
· forbidden (instantiation.md, tools/instantiate/*).

## Vorlaeufige Klassifikation

Alle Befunde waren Box-Artefakt-Versionsdrift (Fall B). Keine Legacy Defects,
keine Known Breaches, keine Migration Candidates im Produktcode.

## Menschliche Entscheidungen

- 2026-06-21: Plan freigegeben, alle Governance-Dateien (Glossare, preflight, …)
  dürfen geändert werden.
- 2026-06-21: glossar-meta.md erhält volle Brownfield-Metabegriffe (nicht nur
  Heading-Umbenennung), da die Box jetzt brownfield-fähig ist.

## Zielmodell / Projektionen

box-python v0.2.7 Regelstand, projiziert in die lokalen Box-Artefakte unter
Erhalt der lokalen operativen Wahrheit (Fachbegriffe, Muster, Tool-Config).

## Uebernommene Aenderungen

- add: BROWNFIELD-MIGRATION.md (Platzhalter ersetzt), .agent-box/migrations/.
- preflight-checkliste.md: Fast-Path P1+P4+P7+P8+P9 (lokale 11-Schritt-Nummerierung,
  da lokaler MODELL-README-Schritt P6 bewahrt), 8 Bedingungen, "Schreibrechte entfällt
  nie", "Brownfield nie SICHER", Eintragstiefe in P5, meta-Routing.
- AGENTS.md / AGENTS-COMPACT.md: glossar-meta in Glossartrennung; SP7/T1 Eintragstiefe;
  Importverletzung SICHER→MITTEL; H3-Erweiterung (migration-bridges); BF1-BF12 Codefamilie;
  Brownfield-Verweis; geschützte Dateien (BROWNFIELD-MIGRATION.md, glossar-meta.md,
  .agent-box/adoption.md, .agent-box/migrations/). Compact-Schutzliste konsistent ergänzt.
- sprechakt-protokoll.md, task-schnitt.md: SP7/T1 Eintragstiefe-Wortlaut.
- erfahrungsbericht-protokoll.md: "Vorgeschlagene Musterkennung", Append-only-Hinweis.
- regelmatrix.md: glossar-meta + BROWNFIELD-MIGRATION als Autorität, Brownfield-Tabellenzeilen.
- package-schema.md: Known-Breach-Format gehärtet (Scope, No-growth, Review/Ablauf, Freigabe).
- migration-bridges.md: H2→H3, Diff-Check-Begründung, glossar-meta-Erwähnungen.
- glossar-README.md: (bereits lokal mit meta-Ladebegrenzung; Eintragstiefe vorhanden).
- learning-matrix.md: Muster-Schwelle 1a normiert, Eingangspfad-Sektion, glossar-Liste.
- glossar-meta.md: Struktur auf ## 3. Begriffe (Checker-Pflicht), volles Eintrag-Format,
  12 Brownfield-Metabegriffe ergänzt, SP7-Bezug. Alle 14 lokalen Begriffe bewahrt.
- .agent-box-template.md: Box-Version v0.2.7, neue Dateien, Adoptionsmarker.
- Tools: check_agent_docs_consistency.py auf v0.2.7-Logik (glossar-meta, BROWNFIELD,
  Projektzustandsmarker-Check); check_import_layers.py v0.2.7 + Dead-Code entfernt;
  resolve_test_obligations.py netto unverändert.

## Bewusst nicht uebernommene Aenderungen

- glossar-domain.md / glossar-system.md: NICHT geändert. Lokal gefüllt mit echten
  Wetter-Fachbegriffen und bereits eigenem Eintragstiefe-Modell — Template-Prosa
  hätte lokale Wahrheit verwässert.
- Lokale Tool-Anpassungen bewahrt: cli→domain (FORBIDDEN_IMPORTS, passt zu lokaler
  package-schema), Default-Modus "instantiated" im docs-Checker, TEST_COMMANDS,
  Docstring-Sentinel <PYTHON_PACKAGE_NAME>.
- migration-bridges.md: lokales "Kein aktiver Bridge-Eintrag" statt Template-BR-001-Beispiel.
- BR-001 Beispiel-Bridge NICHT reaktiviert.

## Legacy Defects

keine festgestellt.

## Known Breaches

keine. (Inventur erzeugt keine Known Breaches.)

## Ausgefuehrte Checks

Baseline (vor Migration): alle grün — siehe oben.
Abschluss (nach Migration, identisch grün):

```text
python tools/check_agent_docs_consistency.py --instantiated   → OK
python tools/check_import_layers.py --preflight src tests tools → OK (38 Dateien)
python tools/resolve_test_obligations.py --selfcheck --instantiated → OK
python -m ruff check . --exclude tmp / format --check         → sauber
python -m mypy src                                            → OK (22 Dateien)
python -m pytest                                              → 64 passed
```

## Offene Warnungen

keine. Hinweis: tmp/brownfield/ (entpacktes Quell-Template) bleibt untracked
und kann nach dem Merge entfernt werden.

## Abschlussentscheidung

Migration v0.2.3 → v0.2.7 erfolgreich abgeschlossen. Lokale operative Wahrheit
vollständig erhalten, kein Test-/Check-Regress gegenüber Baseline.
